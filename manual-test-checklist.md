# Whisper Recorder 手動テストチェックリスト

## 0. 事前準備
- `.env` に `OPENAI_API_KEY` が設定されていること
- 依存が未導入なら `pip install -r requirements.txt`
- 既存データ影響を避ける場合は以下を退避/削除  
  - `history.json`  
  - `settings.json`

## 1. 起動確認
1. `python app.py` を実行する
2. ウィンドウが開くこと
3. タスクトレイにアイコンが出ること

期待結果:
- 起動時にエラー終了しない

## 2. 死にUI削除の確認
1. ホーム画面を開く
2. 以下が表示されないことを確認
   - 「人気の使用例」
   - 「レポートを見る」
   - フィードバック入力ブロック
3. 設定画面を開く
4. 以下が表示されないことを確認
   - 「起動時に開く」
   - 「ヘルプセンター」
   - 「リリースノート」

期待結果:
- 削除対象UIが残っていない

## 3. 履歴表示（実データ化）確認
1. 何か短文を音声入力する（ホットキー長押し→離す）
2. 履歴画面を開く
3. 入力結果が履歴に1件追加されること
4. アプリを再起動し、履歴が残っていること

期待結果:
- 履歴が固定サンプルではなく実データで表示される
- 再起動後も履歴が保持される

## 4. 履歴保持期間の確認
1. 履歴画面で「履歴を保持」を `7日間` に変更
2. アプリ再起動後も `7日間` が選択されていること
3. `永遠に` へ変更し、再起動後に保持されていること

期待結果:
- 選択値が保存される

## 5. ホットキー設定の確認
1. 設定画面で `Left Alt` を選択
2. `Left Alt` で録音開始/終了できること
3. `Right Alt` に戻し、`Right Alt` で録音できること
4. `Left/Right Alt` を選択し、両方で録音できること
5. ホーム画面のキー表示が設定値に追従すること

期待結果:
- 設定したホットキーのみ有効（または両方）
- 表示ラベルも一致

## 6. 言語設定の確認
1. 設定画面で言語を `English` に変更
2. 英語で短文を音声入力する
3. 日本語に戻して同様に入力する

期待結果:
- 言語変更後もエラーなく文字起こしできる

## 7. 既存機能の回帰確認
1. 辞書画面で単語を追加/削除する
2. スニペット画面でキーワードを追加/削除する
3. スニペットキーワードを音声で発話し、定型文展開されること

期待結果:
- 辞書/スニペット機能が従来通り動作

## 8. 異常系（最低限）
1. ネットワーク遮断状態で音声入力する
2. マイク未接続または利用不可状態で録音開始する

期待結果:
- アプリがクラッシュせず、通知で失敗が分かる
